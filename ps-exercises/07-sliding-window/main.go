package main

import "fmt"

func bestWindow(values []int, size int) (start, total int, ok bool) {
	// TODO: calculate first window, then slide.
	return 0, 0, false
}

func main() {
	fmt.Println(bestWindow([]int{4, 2, 7, 1, 8}, 3))
}
