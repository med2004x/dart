-- 1. Tasks with no due date.

-- 2. Users with at least one open task using EXISTS.

-- 3. Projects with no tasks using NOT EXISTS.

-- 4. Each task plus project task count using a window.

-- 5. Top two priority tasks per project using row_number().

-- 6. Running task count ordered by created_at and id.
